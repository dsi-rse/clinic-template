### Scripts directory

This directory contains runnable Python scripts. Each script should use `argparse`, import its core functionality from `{{ cookiecutter.code_directory }}`, and be documented here and in the root README.md.
